ldap:
  url: ldap://%host:389/
  intra_url: ldap://%host:389/

  bind_dn: %dn
  bind_pw:  %pwd
  base_dn: OU=xxx,dc=domain,dc=oceansoft

  filter: (&(objectCategory=person)(objectClass=user)(memberOf=cn=BDAI,OU=组,dc=domain,dc=oceansoft))
  attrs: [sAMAccountName, mail, mobile]

role:
  g_dev:
    user: []
    workspace: /ws.g_dev
    hdfs_workspace: /ws.g_dev
  g_prod:
    user: [prodrt, prodol]
    workspace: /ws.g_prod
    hdfs_workspace: /ws.g_prod

ansible:
  inventory: /etc/ansible/hosts
  todo: {{ cwd }}/output/ansible/group_user.playbook.yml
  cdh_host_pattern: uydc

kerberos:
  realm: UYDC.COM
  admin: root/admin
  admin_pw: Uydc.2910
  keytab_output_to: {{ cwd }}/output/kerberos
  todo: {{ cwd }}/output/kerberos/op_principle.sh

mail:
  server: %server
  username: %username
  password: %pwd
  sender: %sender

hdfs:
  credential:
    super: hdfs
    keytab: {{ cwd }}/conf/hdfs.keytab
  todo: {{ cwd }}/output/hdfs/op_hdfs.sh

